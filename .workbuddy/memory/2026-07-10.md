# 2026-07-10 工作日志

## 网站静态化改造
将 FoxMC（小狐狸生存服）Minecraft 服务器网站从 PHP+MySQL 动态站点转换为纯静态 HTML 站点。

### 改动内容
1. **新建 `js/data.js`** - 将所有 CMS 内容（站点设置、Hero、配置、帮助、特色、相册、团队、社区、页脚、公告、服务器状态、联系表单配置）从 PHP 数据库提取为静态 JS 对象。可编辑此文件来修改网站内容。
2. **重写 `js/script.js`** - 移除所有对 `admin/public_api.php` 和 `user/api/index.php` 的 fetch 调用：
   - UserSession: 已移除（不需要登录）
   - Announce: 从 `data.js` 读取公告数据
   - CMS: 直接从 `window.__FOXMC_STATIC_DATA__` 读取所有内容
   - ServerStatus: 支持静态文案模式（data.js 配置）或 API 模式
   - ContactForm: 支持 mailto（默认）/ Formspree / 关闭三种模式
3. **修改 `index.html`** - 添加 `data.js` 引用，移除导航栏中指向 `user/panel.php?tab=shop` 的 PHP 商城链接
4. **保留 `admin/` 和 `user/` 目录** - 如果后续部署 PHP 后端，这些功能仍可恢复使用

### 使用方法
- 编辑 `js/data.js` 即可修改网站所有内容（IP 地址、Logo、文字、图片、团队等）
- 直接用任何静态文件服务器（Nginx/Apache/GitHub Pages）部署
- 联系表单默认使用 mailto 协议，可在 data.js 中切换为 Formspree

## 品牌更新
- 从"小狐狸生存服"改为"橙猫生存服务器"
- 服务器 IP: mc.ambercat.top（Java版）/ be.ambercat.top:40078（基岩版）
- QQ群: 992719293
- 白名单服务器，需通过QQ群申请
- 信息来源: https://www.wdsjfwq.com/server-1685.html
- 站主邮箱: admin@ambercat.top（腐竹）/ 3665822645@qq.com（管理员）

## UI 调整
- 移除导航栏"登录/注册"按钮及对应 UserSession JS 模块
- 移除"服务器信息"板块四个卡片的图标（CPU/RAM/网络/存储）
- "如何加入服务器"三个步骤卡片统一高度（align-items: stretch + flex 布局）
- Java版地址去掉端口号（mc.ambercat.top 而非 mc.ambercat.top:25565）
- 社区板块：移除"官网访问"卡片，QQ 群二维码替换为真实图片

## 目录重构（Cloudflare Pages 部署适配）
```
MCWEBSI/
├── index.html             # 网站首页
├── players.html           # 在线玩家页面
├── css/style.css          # 样式表
├── js/data.js             # 静态内容配置
├── js/script.js           # 前台 JS
├── functions/api/mcs/[[path]].js   # CORS 代理 Function
├── _routes.json           # 限制 Function 仅在 /api/* 触发
├── png/ egg/ assets/      # 图片资源
├── admin/ user/           # PHP 后台（不参与静态部署）
```
Cloudflare Pages 设置: Framework=None, Build command=空, Output directory=/

## 接入服务器真实在线数据
- API: GET http://chengmao.jkun.cf:36779/open-api/players → 返回 { code, players: [{name, isOnline, uuid, gamemode}] }
- 浏览器直连会被 CORS 拦截，故通过 Cloudflare Pages Function 代理
- 前端请求: `/api/mcs/open-api/players` → Function 转发到上游并设置 CORS 头
- 在线人数 = 过滤 players 数组中 isOnline: true 的数量
- 状态点统一绿色，30s 周期刷新
- 主页 hero 状态文字可点击跳转到 players.html

## 同步 HTML 硬编码内容（消除"加载旧内容再刷新"）
- 把所有区块的硬编码内容（标题/描述/IP/团队人数/友链）都同步为 data.js 中的值
- Hero 状态文字改为"加载中…"，由 JS 异步替换为真实在线人数

## 玩家页面特性 (players.html)
- 圆形头像（mc-heads.net/avatar/）+ 玩家名 + 游戏模式
- 点击头像弹出 3D 皮肤预览（skinview3d + Three.js）
- 非正版玩家显示"无法查看皮肤"（uapis.cn 验证 UUID）
- 拖拽旋转 + 滚轮缩放控制（OrbitControls）
- WalkingAnimation 让模型走路
- 30 秒自动刷新 + 手动刷新按钮
- 4 种状态 UI：加载中/正常/离线/错误

## 站点图标
- 用户提供的"贺新春小木屋"PNG 作为 favicon 和 logo
- 文件: png/icon.png（已删除旧 favicon.png）
- 引用: index.html 和 players.html 全部用 png/icon.png

## 友情链接
- 数据来源: https://www.wdsjfwq.com/server-1685.html
- "我的世界官网" + "我的世界国际服" + "服务器网页地图" + "服务器站"
